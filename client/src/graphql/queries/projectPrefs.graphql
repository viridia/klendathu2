query projectPrefsQuery($project: ID!) {
  projectPrefs(project: $project) {
    columns
    labels
    # labelProps {
    #   id
    #   name
    #   color
    # }
    filters {
      name
      value
    }
  }
}
