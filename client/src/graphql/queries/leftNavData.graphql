query leftNavQuery($project: ID!) {
  projectPrefs(project: $project) {
    labels
    labelProps {
      id
      name
      color
    }
    filters {
      name
      value
    }
  }
  projects {
    id
    name
    title
  }
}
